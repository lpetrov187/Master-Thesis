[project]
name = "master-rad-agent"
version = "0.1.0"
requires-python = ">=3.14"

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
